---
interface Props {
  src: string;
  alt?: string;
}

const { src, alt } = Astro.props;
---
<img
  src={src}
  alt={alt}
  loading="lazy"
  class="spek-zoomable rounded-md border border-border-subtle cursor-zoom-in transition-opacity hover:opacity-90"
/>
