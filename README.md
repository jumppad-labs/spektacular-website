# spektacular-website

## Local development

Serve the site locally with Python:

```
python3 -m http.server 8080
```

Then open `http://localhost:8080` in a browser.

> Opening `.html` files directly (`file:///...`) works for layout but copy
> buttons require localhost or HTTPS to access the clipboard API.
